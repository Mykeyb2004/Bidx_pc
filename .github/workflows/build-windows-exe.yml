name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows-exe:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: uv sync --dev

      - name: Build EXE with PyInstaller
        shell: pwsh
        run: |
          uv run --with pyinstaller pyinstaller `
            --noconfirm `
            --clean `
            --onefile `
            --windowed `
            --name bid-writer `
            --collect-all ttkbootstrap `
            run.py

      - name: Prepare artifact bundle
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path artifact | Out-Null
          Copy-Item dist/bid-writer.exe artifact/
          Copy-Item config.example.yaml artifact/
          if (Test-Path .env.example) {
            Copy-Item .env.example artifact/
          }
          if (Test-Path README.md) {
            Copy-Item README.md artifact/
          }

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: bid-writer-windows-exe
          path: artifact/
          if-no-files-found: error

      - name: Create release package
        if: github.ref_type == 'tag'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path release | Out-Null
          Compress-Archive `
            -Path artifact/* `
            -DestinationPath release/bid-writer-${{ github.ref_name }}-windows.zip `
            -Force

      - name: Publish GitHub release
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: release/bid-writer-${{ github.ref_name }}-windows.zip
