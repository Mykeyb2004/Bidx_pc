---
phase: 01-prompt-contract-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/prompt_contract.md
autonomous: true
requirements:
  - PRMT-01
  - PRMT-02
user_setup: []
must_haves:
  truths:
    - docs/prompt_contract.md states the exact Phase 1 business-block contract, the prompt-section order, and the section-to-block rollup for one chapter generation
    - docs/prompt_contract.md identifies which existing code paths and trace fields own prompt assembly, contract summarization, and context provenance
  artifacts:
    - docs/prompt_contract.md exists with current-state audit and target-contract sections
  key_links:
    - docs/prompt_contract.md maps each business block to bid_writer/ai_writer.py section names and bid_writer/generation_trace.py artifact fields
---

<objective>
Audit the current prompt assembly path and define the explicit Phase 1 prompt contract.

Purpose: Give execution a single source of truth for what the prompt contract is before any code refactor starts.
Output: A maintainer-facing contract document in `docs/prompt_contract.md` that captures current behavior and the exact target rollup model for Phase 1.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-prompt-contract-baseline/01-CONTEXT.md
@.planning/phases/01-prompt-contract-baseline/01-RESEARCH.md
@docs/generation_trace.md
@README.md
@bid_writer/ai_writer.py
@bid_writer/generation_trace.py
@bid_writer/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the current prompt-assembly and trace surface in one document</name>
  <files>docs/prompt_contract.md</files>
  <read_first>bid_writer/ai_writer.py, bid_writer/generation_trace.py, bid_writer/config.py, docs/generation_trace.md, README.md, .planning/phases/01-prompt-contract-baseline/01-RESEARCH.md</read_first>
  <action>Create `docs/prompt_contract.md` with the exact sections `## Current prompt assembly path`, `## Current low-level prompt sections`, `## Current trace artifact surface`, and `## Current config compatibility surface`. In `## Current prompt assembly path`, document the exact `AIWriter.build_prompt_result(...)` order that exists today: `task_card`, `structure_contract`, optional `first_line_rule`, pruned-context branch (`scope_reference`, `scoring_focus`, `requirement_brief` or `requirement_points`) or full-context branch (`full_outline`, `bid_requirements`, `scoring_criteria`), optional `additional_requirements`, and optional `extra_rules`. In the trace section, document that `GenerationTraceSession._build_context_payload()` currently writes `context_mode`, `context_pruning_enabled`, `prompt_sections`, `prompt_lengths`, and either `pruned_context` or `full_context`. In the compatibility section, record the current public config owners exactly as they exist now: `prompt.*`, `context_pruning.*`, `generation_trace.*`, root-level `outline_file` / `bid_requirements` / `scoring_criteria`, and `inputs.*` fallbacks.</action>
  <verify>rg -n "Current prompt assembly path|task_card|structure_contract|prompt_sections|context_pruning|generation_trace" docs/prompt_contract.md</verify>
  <acceptance_criteria>
    - docs/prompt_contract.md contains `## Current prompt assembly path`
    - docs/prompt_contract.md contains `task_card`, `structure_contract`, `first_line_rule`, `additional_requirements`, and `extra_rules`
    - docs/prompt_contract.md contains `prompt_sections`, `pruned_context`, and `full_context`
    - docs/prompt_contract.md contains `prompt.*`, `context_pruning.*`, and `generation_trace.*`
  </acceptance_criteria>
  <done>`docs/prompt_contract.md` explains the current prompt path, trace payload, and config surface without requiring the executor to read multiple modules first.</done>
</task>

<task type="auto">
  <name>Task 2: Define the exact Phase 1 business-block contract and rollup rules</name>
  <files>docs/prompt_contract.md</files>
  <read_first>docs/prompt_contract.md, .planning/phases/01-prompt-contract-baseline/01-CONTEXT.md, .planning/phases/01-prompt-contract-baseline/01-RESEARCH.md, bid_writer/ai_writer.py, bid_writer/generation_trace.py</read_first>
  <action>Append the exact sections `## Phase 1 business blocks` and `## Rollup and provenance rules` to `docs/prompt_contract.md`. Define these six business-block IDs verbatim: `system_constraints`, `chapter_task`, `structure_rules`, `chapter_scope`, `requirement_context`, `scoring_context`. Specify the exact rollup membership rules: `chapter_task` includes `task_card` and `additional_requirements`; `structure_rules` includes `structure_contract`, `first_line_rule`, and `extra_rules`; `chapter_scope` includes `scope_reference` or `full_outline`; `requirement_context` includes `requirement_brief`, `requirement_points`, and `bid_requirements`; `scoring_context` includes `scoring_focus` and `scoring_criteria`; `system_constraints` is sourced from `AIWriter.build_system_prompt()` plus `Config.role`, `prompt_bidder_name`, `prompt_allow_markdown_headings`, `prompt_allow_english_terms`, and `prompt_hard_constraints`. Record that low-level `prompt_sections` remain preserved unchanged under the new summary layer and that every business block must expose a `source_context` list naming its exact inputs.</action>
  <verify>rg -n "Phase 1 business blocks|system_constraints|chapter_task|structure_rules|chapter_scope|requirement_context|scoring_context|source_context" docs/prompt_contract.md</verify>
  <acceptance_criteria>
    - docs/prompt_contract.md contains `## Phase 1 business blocks`
    - docs/prompt_contract.md contains all six exact block IDs: `system_constraints`, `chapter_task`, `structure_rules`, `chapter_scope`, `requirement_context`, `scoring_context`
    - docs/prompt_contract.md contains the exact low-level section IDs `task_card`, `structure_contract`, `scope_reference`, `requirement_brief`, `requirement_points`, `bid_requirements`, `scoring_focus`, `scoring_criteria`, `additional_requirements`, and `extra_rules`
    - docs/prompt_contract.md contains the exact key name `source_context`
  </acceptance_criteria>
  <done>`docs/prompt_contract.md` defines a stable Phase 1 contract that later plans can implement and verify mechanically.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "Current prompt assembly path|Phase 1 business blocks|source_context" docs/prompt_contract.md` returns matches
- [ ] `rg -n "system_constraints|chapter_task|structure_rules|chapter_scope|requirement_context|scoring_context" docs/prompt_contract.md` returns all six block IDs
- [ ] The document describes both the current raw `prompt_sections` layer and the future business-block summary layer
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `docs/prompt_contract.md` is sufficient for a maintainer to understand the contract without reading multiple source modules first
  </success_criteria>

<output>
After completion, create `.planning/phases/01-prompt-contract-baseline/01-01-SUMMARY.md`
</output>
