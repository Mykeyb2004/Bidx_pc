---
phase: 01-prompt-contract-baseline
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - pyproject.toml
  - README.md
  - tests/test_prompt_contract.py
  - tests/fixtures/legacy_prompt_config.yaml
  - tests/fixtures/current_prompt_config.yaml
  - tests/fixtures/outline.md
  - tests/fixtures/bid_requirements.md
  - tests/fixtures/scoring_criteria.md
autonomous: true
requirements:
  - PRMT-03
user_setup: []
must_haves:
  truths:
    - Existing-style YAML configs without new prompt-contract fields still load through Config and can build a prompt successfully
    - Automated tests prove the new `prompt_contract` summary coexists with the pre-existing raw `prompt_sections` trace payload
    - Maintainers can run the regression checks with `uv run pytest` and find the compatibility guidance in README.md
  artifacts:
    - tests/test_prompt_contract.py exists and covers compatibility plus prompt-contract trace behavior
    - tests/fixtures/legacy_prompt_config.yaml and tests/fixtures/current_prompt_config.yaml exist with local offline fixture paths
    - pyproject.toml declares pytest in the project test/development dependency surface
  key_links:
    - tests/test_prompt_contract.py exercises Config, AIWriter, and GenerationTraceSession together against the local fixture configs
---

<objective>
Add backward-compatibility coverage for existing YAML prompt configs.

Purpose: Turn the Phase 1 compatibility promise into executable regression checks instead of relying on manual inspection.
Output: A minimal pytest harness, offline fixture configs, and maintainer docs for running the compatibility checks.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-prompt-contract-baseline/01-CONTEXT.md
@.planning/phases/01-prompt-contract-baseline/01-RESEARCH.md
@.planning/phases/01-prompt-contract-baseline/01-VALIDATION.md
@docs/prompt_contract.md
@docs/generation_trace.md
@README.md
@pyproject.toml
@config_公共服务满意度.yaml
@bid_writer/ai_writer.py
@bid_writer/config.py
@bid_writer/generation_trace.py
@bid_writer/outline_parser.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pytest and offline config fixtures for prompt-contract coverage</name>
  <files>pyproject.toml, tests/fixtures/legacy_prompt_config.yaml, tests/fixtures/current_prompt_config.yaml, tests/fixtures/outline.md, tests/fixtures/bid_requirements.md, tests/fixtures/scoring_criteria.md</files>
  <read_first>pyproject.toml, README.md, config_公共服务满意度.yaml, bid_writer/config.py, .planning/phases/01-prompt-contract-baseline/01-VALIDATION.md</read_first>
  <action>Update `pyproject.toml` to declare `pytest>=8.3` in a uv-compatible development/test dependency entry so the repository can run `uv run pytest`. Then create local fixture files under `tests/fixtures/`: `outline.md`, `bid_requirements.md`, `scoring_criteria.md`, `legacy_prompt_config.yaml`, and `current_prompt_config.yaml`. The legacy fixture must use the old root-level config shape (`outline_file`, `bid_requirements_file` or inline `bid_requirements`, `scoring_criteria_file` or inline `scoring_criteria`) and omit any future prompt-contract keys. The current-like fixture must mirror the checked-in config surface from `config_公共服务满意度.yaml` for `prompt.*`, `context_pruning.*`, and `generation_trace.*`, but every referenced file path must point to the new local fixture files so tests run offline and deterministically.</action>
  <verify>rg -n "pytest" pyproject.toml && rg -n "outline_file|generation_trace:|context_pruning:|prompt:" tests/fixtures/*.yaml</verify>
  <acceptance_criteria>
    - pyproject.toml contains `pytest`
    - tests/fixtures/legacy_prompt_config.yaml contains `outline_file`
    - tests/fixtures/current_prompt_config.yaml contains `prompt:`, `context_pruning:`, and `generation_trace:`
    - tests/fixtures/outline.md, tests/fixtures/bid_requirements.md, and tests/fixtures/scoring_criteria.md exist
  </acceptance_criteria>
  <done>The repository has a local, offline fixture set that can exercise both legacy-style and current-style config loading.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic tests for compatibility and trace contract behavior</name>
  <files>tests/test_prompt_contract.py</files>
  <read_first>bid_writer/ai_writer.py, bid_writer/config.py, bid_writer/generation_trace.py, bid_writer/outline_parser.py, tests/fixtures/legacy_prompt_config.yaml, tests/fixtures/current_prompt_config.yaml, tests/fixtures/outline.md, tests/fixtures/bid_requirements.md, tests/fixtures/scoring_criteria.md</read_first>
  <action>Create `tests/test_prompt_contract.py` with deterministic pytest cases that do not call the network. Add one test that loads `tests/fixtures/legacy_prompt_config.yaml`, parses a heading from `tests/fixtures/outline.md`, constructs `Config`, and asserts `AIWriter.build_prompt_result(...)` returns a non-empty prompt even though no new prompt-contract fields exist. Add a second test that loads `tests/fixtures/current_prompt_config.yaml` and asserts `PromptBuildResult.prompt_contract_blocks` exposes these six exact block IDs in order: `system_constraints`, `chapter_task`, `structure_rules`, `chapter_scope`, `requirement_context`, `scoring_context`. Add a third test that creates a temporary trace directory, starts a `GenerationTraceSession`, reads `02_context_assembly.json`, and asserts the JSON contains both a top-level `prompt_contract` object and the pre-existing `prompt_sections` array. Stub or monkeypatch the OpenAI client construction if needed so the tests only exercise prompt building and trace serialization.</action>
  <verify>uv run pytest tests/test_prompt_contract.py -q</verify>
  <acceptance_criteria>
    - tests/test_prompt_contract.py contains `legacy_prompt_config.yaml` and `current_prompt_config.yaml`
    - tests/test_prompt_contract.py contains all six exact block IDs `system_constraints`, `chapter_task`, `structure_rules`, `chapter_scope`, `requirement_context`, and `scoring_context`
    - tests/test_prompt_contract.py asserts both `prompt_contract` and `prompt_sections`
    - `uv run pytest tests/test_prompt_contract.py -q` exits 0
  </acceptance_criteria>
  <done>Compatibility and trace-contract behavior are guarded by executable regression tests instead of manual assumptions.</done>
</task>

<task type="auto">
  <name>Task 3: Document the compatibility guarantee and regression command</name>
  <files>README.md</files>
  <read_first>README.md, docs/prompt_contract.md, docs/generation_trace.md, tests/test_prompt_contract.py</read_first>
  <action>Add a short maintainer section to `README.md` stating that the prompt contract remains code-defined, existing YAML prompt fields continue to load, and the regression command is exactly `uv run pytest tests/test_prompt_contract.py -q`. If Phase 2 introduces deprecation warnings for reorganized fields, document the warning surface there instead of promising field removal. Keep this section near the existing configuration guidance so maintainers find it while editing configs.</action>
  <verify>rg -n "uv run pytest tests/test_prompt_contract.py -q|prompt contract|existing YAML" README.md</verify>
  <acceptance_criteria>
    - README.md contains `uv run pytest tests/test_prompt_contract.py -q`
    - README.md contains `prompt contract`
    - README.md contains `existing YAML`
  </acceptance_criteria>
  <done>Maintainers can see both the compatibility promise and the exact regression command in the main repository documentation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_prompt_contract.py -q` succeeds
- [ ] `uv run pytest -q` succeeds
- [ ] `rg -n "uv run pytest tests/test_prompt_contract.py -q|prompt contract|existing YAML" README.md` returns matches
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Prompt-contract changes are protected by automated compatibility coverage instead of manual spot checks
  </success_criteria>

<output>
After completion, create `.planning/phases/01-prompt-contract-baseline/01-03-SUMMARY.md`
</output>
