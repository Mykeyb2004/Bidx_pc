---
phase: 01-prompt-contract-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - bid_writer/ai_writer.py
  - bid_writer/generation_trace.py
  - docs/generation_trace.md
autonomous: true
requirements:
  - PRMT-01
  - PRMT-02
user_setup: []
must_haves:
  truths:
    - bid_writer/ai_writer.py produces explicit prompt-contract block metadata with stable IDs and ordering while preserving the existing low-level prompt section list
    - bid_writer/generation_trace.py writes a maintainer-facing `prompt_contract` summary with source-context provenance while preserving the existing `prompt_sections` payload and artifact filenames
    - docs/generation_trace.md explains the new summary layer without removing the current raw-trace workflow
  artifacts:
    - bid_writer/ai_writer.py contains the explicit block definitions and rollup logic
    - bid_writer/generation_trace.py contains `prompt_contract` serialization and summary rendering
    - docs/generation_trace.md documents the contract summary field
  key_links:
    - GenerationTraceSession serializes the same block IDs and source-context lists that AIWriter assembles
---

<objective>
Refactor prompt-section construction so ordering, labels, and trace-facing defaults are explicit.

Purpose: Convert the Phase 1 contract from a design document into code and maintainer-facing artifacts without changing the runtime prompt behavior more than necessary.
Output: Explicit contract metadata in prompt assembly, trace payload updates, and updated maintainer documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-prompt-contract-baseline/01-CONTEXT.md
@.planning/phases/01-prompt-contract-baseline/01-RESEARCH.md
@.planning/phases/01-prompt-contract-baseline/01-VALIDATION.md
@docs/prompt_contract.md
@docs/generation_trace.md
@bid_writer/ai_writer.py
@bid_writer/generation_trace.py
@bid_writer/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the prompt contract explicit in AIWriter</name>
  <files>bid_writer/ai_writer.py</files>
  <read_first>bid_writer/ai_writer.py, docs/prompt_contract.md, .planning/phases/01-prompt-contract-baseline/01-CONTEXT.md, .planning/phases/01-prompt-contract-baseline/01-RESEARCH.md</read_first>
  <action>Refactor `bid_writer/ai_writer.py` so the prompt contract is code-defined rather than implicit in scattered append calls. Add explicit metadata for the six Phase 1 business blocks exactly as documented in `docs/prompt_contract.md`: `system_constraints`, `chapter_task`, `structure_rules`, `chapter_scope`, `requirement_context`, and `scoring_context`. Extend `PromptBuildResult` with a field such as `prompt_contract_blocks` that stores, for each block, the exact keys `id`, `label`, `prompt_kind`, `section_names`, `source_context`, and `chars`. Build this block metadata from the real low-level section IDs that already exist today: `task_card` plus `additional_requirements` in `chapter_task`; `structure_contract`, `first_line_rule`, and `extra_rules` in `structure_rules`; `scope_reference` or `full_outline` in `chapter_scope`; `requirement_brief`, `requirement_points`, and `bid_requirements` in `requirement_context`; `scoring_focus` and `scoring_criteria` in `scoring_context`. Represent `system_constraints` as a synthetic block derived from `build_system_prompt()` inputs and `Config` prompt fields rather than duplicating the full system prompt text. Keep the final prompt string assembly behavior unchanged except for the new explicit contract bookkeeping.</action>
  <verify>rg -n "prompt_contract_blocks|system_constraints|chapter_task|structure_rules|chapter_scope|requirement_context|scoring_context|source_context" bid_writer/ai_writer.py</verify>
  <acceptance_criteria>
    - bid_writer/ai_writer.py contains `prompt_contract_blocks`
    - bid_writer/ai_writer.py contains all six exact block IDs `system_constraints`, `chapter_task`, `structure_rules`, `chapter_scope`, `requirement_context`, and `scoring_context`
    - bid_writer/ai_writer.py still contains the low-level section names `task_card`, `structure_contract`, `scope_reference`, `requirement_brief`, `requirement_points`, `bid_requirements`, `scoring_focus`, `scoring_criteria`, `additional_requirements`, and `extra_rules`
    - bid_writer/ai_writer.py contains the exact key name `source_context`
  </acceptance_criteria>
  <done>`PromptBuildResult` exposes a stable high-level contract while preserving the raw prompt assembly details that existing trace tooling already expects.</done>
</task>

<task type="auto">
  <name>Task 2: Persist the contract summary in trace artifacts and maintainer docs</name>
  <files>bid_writer/generation_trace.py, docs/generation_trace.md</files>
  <read_first>bid_writer/generation_trace.py, docs/generation_trace.md, bid_writer/ai_writer.py, docs/prompt_contract.md</read_first>
  <action>Update `bid_writer/generation_trace.py` so `GenerationTraceSession._build_context_payload()` writes a new top-level `prompt_contract` object into `02_context_assembly.json` and `GenerationTraceSession._build_summary()` lists the business-block order in `07_summary.md`. Each serialized block entry must include the exact keys `id`, `label`, `prompt_kind`, `section_names`, `source_context`, and `chars`. Keep the existing `prompt_sections`, `context_mode`, `pruned_context`, `full_context`, `03_prompt_system.md`, `04_prompt_user.md`, and other artifact filenames unchanged. Then update `docs/generation_trace.md` to document the new `prompt_contract` field, explain that `prompt_sections` remains the detailed raw layer, and revise the recommended review order so maintainers look at the business-block summary first and then drill into the raw section list only when they need deeper debugging.</action>
  <verify>rg -n "prompt_contract|source_context|prompt_sections" bid_writer/generation_trace.py docs/generation_trace.md</verify>
  <acceptance_criteria>
    - bid_writer/generation_trace.py contains the exact key `prompt_contract`
    - bid_writer/generation_trace.py contains the exact keys `id`, `label`, `prompt_kind`, `section_names`, `source_context`, and `chars`
    - bid_writer/generation_trace.py still contains `prompt_sections`, `pruned_context`, and `full_context`
    - docs/generation_trace.md contains `prompt_contract` and `prompt_sections`
  </acceptance_criteria>
  <done>The trace layer exposes a maintainable summary view while keeping backward-compatible raw detail for debugging.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run python -m compileall bid_writer run.py` succeeds
- [ ] `rg -n "prompt_contract|source_context|prompt_sections" bid_writer/generation_trace.py docs/generation_trace.md` returns matches
- [ ] `rg -n "prompt_contract_blocks|system_constraints|chapter_task|structure_rules|chapter_scope|requirement_context|scoring_context" bid_writer/ai_writer.py` returns matches
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- One chapter generation can be understood from the new business-block summary without losing access to the low-level section list
  </success_criteria>

<output>
After completion, create `.planning/phases/01-prompt-contract-baseline/01-02-SUMMARY.md`
</output>
